import { Router } from "express";

const conversationsRouter = Router();

// conversationsRouter.post("/create");
// conversationsRouter.get("/question");
// conversationsRouter.get("/question/:id");
// conversationsRouter.get("/history");

export default conversationsRouter;
